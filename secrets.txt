gitginore .env

npm install dotenv

cria .env

nome chave = escolhe nome

cp .env.example .env

require('dotenv').config();

const dbPassword = process.env.DB_PASSWORD;

if (!dbPassword) {
  throw new Error("DB_PASSWORD is not defined");
}

.env fica uma pasta atras